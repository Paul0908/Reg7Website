import {LazyImgLoadingDirective} from './lazy-img-loading.directive';

describe('LazyImgLoadingDirective', () => {
  it('should create an instance', () => {
    const directive = new LazyImgLoadingDirective();
    expect(directive).toBeTruthy();
  });
});
